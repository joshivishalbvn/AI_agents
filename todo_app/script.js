let todoList = []; const todoInput = document.getElementById("todo-input"); const addTodoButton = document.getElementById("add-todo"); const todoListElement = document.getElementById("todo-list"); addTodoButton.addEventListener("click", addTodo); function addTodo() { const newTodo = todoInput.value; if (newTodo.trim() === "") { alert("Please enter a todo item"); return; } todoList.push(newTodo); todoInput.value = ""; renderTodoList(); } function renderTodoList() { const todoListHtml = todoList.map((todo, index) => `<li>${todo} <button class="remove-todo" data-index="${index}">Remove</button></li>`).join(""); todoListElement.innerHTML = todoListHtml; const removeTodoButtons = document.getElementsByClassName("remove-todo"); Array.from(removeTodoButtons).forEach((button) => { button.addEventListener("click", removeTodo); }); } function removeTodo(event) { const index = event.target.dataset.index; todoList.splice(index, 1); renderTodoList(); }
